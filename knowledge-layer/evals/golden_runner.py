"""Golden-set evaluation harness (task P2-3).

STATUS: stub. Contract: run every golden_set question through
retrieval + synthesis, validate citations with citation_validator, write a
golden_runs row per question with run_label = prompt/model/pipeline versions.
Runs on every retrieval/synthesis change; regression blocks deploy (§7.2).
"""

from __future__ import annotations


def main() -> None:
    raise SystemExit("golden_runner: not yet implemented (Phase 2, task P2-3).")


if __name__ == "__main__":
    main()
