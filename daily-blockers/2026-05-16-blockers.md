# Morning Blockers — Sat 16 May 2026

Items only Anthony can action. Diffed against yesterday's list
(`2026-05-15-blockers.md`). Only one commit landed since yesterday
morning (`e4d24a49` — FAQ `data-track` move), so most of the list
carries forward unchanged. Where I could verify code state, I did.

> **Memory file note:** `project_needs_from_anthony_may13.md` still
> isn't surfaced in this workspace. Statuses below are inferred from
> the repo at HEAD (uncommitted blog edits ignored). If the canonical
> memory file lives elsewhere, point me at it and I'll re-diff.

---

## Active blockers (still need you)

### 1. Method course video scripts — STILL OUTSTANDING
- `src/lib/method/modules.ts` still has `videoYouTubeId: null` for
  **all 12 Roadman Method modules** (lines 66, 101, 138, 173, 208,
  248, 284, 319, 353, 388, 423, 459). No change since yesterday.
- The 6 short-form scripts in `docs/teleprompter-scripts-13-may.md`
  and `docs/go-video-script.md` are still the only scripted assets —
  none of the 12 module long-forms (10–15 min each) are written.
- **Decision needed:** which modules already have rough scripts vs.
  need writing from scratch, plus a target batch-record date. The 12
  module slugs are `01-where-you-actually-are` through
  `12-not-done-yet` if you want to mark them inline.

### 2. Beehiiv build for plateau sequences — STILL OUTSTANDING
- Copy ready: `docs/plateau-diagnostic-email-sequences-v2.md`
  (28 emails = 4 sequences × 7 emails over 14 days, segmented by
  `under-recovered` / `no-mans-land` / `strength-gap` /
  `fuelling-deficit`). `plateau-diagnostic-nurture-sequence.md` (v1,
  5 emails) is still in the repo root if you want to keep v1 as a
  fallback — assumption is v2 supersedes.
- The site-side wiring is fine: `/api/newsletter` calls
  `subscribeToBeehiiv` and forwards `source` as a tag.
- **Still outstanding:** building the four automations inside Beehiiv
  with the Day 0/+2/+4/+7/+9/+12/+14 cadence and the four limiter
  tags as triggers.
- **Decision needed:** confirm you or Sarah owns the Beehiiv build,
  and that v2 supersedes v1.

### 3. Exit intent + Masters Report Beehiiv wiring — STILL OUTSTANDING
- Code-side is wired and correct:
  - `src/app/go/_components/GoExitIntent.tsx` posts to
    `/api/newsletter` with `source: "go-exit-intent"`.
  - `src/app/masters-report/_components/MastersReportCapture.tsx`
    posts with `source: "masters-report"`.
  - `src/app/api/newsletter/route.ts` strips the leading slash and
    appends the source as a Beehiiv tag on top of `saturday-spin`.
- **What's still outstanding inside Beehiiv:** confirm `go-exit-intent`
  and `masters-report` segments exist, and that the welcome /
  delivery automations on each segment are switched on. The TODO in
  `MastersReportCapture.tsx` flags this explicitly (lines 18–23).
- **Decision needed:** sign off on the segment names + which
  automations should fire for each tag.

### 4. Method onboarding quiz — READY FOR YOUR FINAL READ
- Route is built and ships behind auth: page at
  `src/app/(method)/method/onboarding/page.tsx`, client components
  (`OnboardingQuiz.tsx`, `QuizProgress.tsx`, `OptionCard.tsx`,
  `ResultsView.tsx`), API at `src/app/api/method/onboarding/route.ts`.
- Logic: 5 steps (goal, hours, level, FTP optional, target event
  optional), with the first three driving the plan recommendation in
  `recommend.ts` against the 48-cell matrix in `plan-matrix.ts`.
- **Decision needed:** read-through of `questions.ts` copy and the
  recommender mapping in `recommend.ts`. Once you green-light, we
  can flip the surface from `robots: noindex` to live and start
  capturing onboarding completion data.

### 5. Stripe / Skool / Beehiiv API keys — STATUS UNCHANGED
- TrainingPeaks API access remains confirmed
  (`docs/trainingpeaks-plan-analysis.md`).
- `.env.example` lists the required slots: `BEEHIIV_API_KEY`,
  `BEEHIIV_PUBLICATION_ID`, `STRIPE_SECRET_KEY`,
  `STRIPE_WEBHOOK_SECRET`, `STRIPE_RACE_REPORT_PRICE_ID`,
  `SKOOL_EMAIL`, `SKOOL_PASSWORD`, `SKOOL_COMMUNITY_SLUG`.
- I still can't see prod `.env` from here, so the question is the
  same as yesterday: which of these are populated in Vercel and
  which still need rotating / generating?

### 6. Test the Google Ads conversion — STILL OUTSTANDING
- Tag + conversion plumbing verified end-to-end:
  - `src/components/analytics/GoogleAdsTag.tsx` loads
    `AW-18123737652` via `Script strategy="afterInteractive"`.
  - `src/components/features/diagnostic/ResultsAnalytics.tsx` fires
    `send_to: "AW-18123737652/WDZ_CNiOvKwcELSUicJD"` on diagnostic
    results view (with a gtag-stub buffer so the event isn't lost
    when the script loads after the effect).
  - `src/app/go/ads/AdsLandingAnalytics.tsx` fires
    `send_to: "AW-18123737652/up0JCJqHxKwcELSUicJD"` once per session
    on ads landing.
- **Action only you can take:** run one Plateau Diagnostic
  end-to-end on prod, then check Google Ads → Conversions for
  "Active" status on both labels (diagnostic complete + ads landing).

---

## Resolved / unchanged from yesterday

### TrainingPeaks legacy plan logic — RESOLVED (carried over)
- Closed yesterday. 222 workouts / 21 session types extracted across
  7 legacy plans; hour-band variants documented in
  `docs/trainingpeaks-hour-band-variants.md`. Building the 48 plan
  variants inside TrainingPeaks is the next *implementation* phase
  but it isn't a blocker on you.

---

## Diff vs. 15 May

| # | Item                                  | 15 May            | 16 May               |
|---|---------------------------------------|-------------------|----------------------|
| 1 | Method module video scripts           | PARTIAL           | PARTIAL (no change)  |
| 2 | Beehiiv build for plateau sequences   | UNRESOLVED        | UNRESOLVED           |
| 3 | Exit intent + Masters Report wiring   | UNRESOLVED        | UNRESOLVED           |
| 4 | Method onboarding quiz                | MOSTLY DONE       | READY FOR FINAL READ |
| 5 | Stripe / Skool / Beehiiv API keys     | PARTIAL           | PARTIAL              |
| 6 | Google Ads conversion test            | UNRESOLVED        | UNRESOLVED           |
| 7 | TrainingPeaks legacy plan logic       | RESOLVED          | RESOLVED (closed)    |

Only one commit shipped overnight (`e4d24a49`: FAQ `data-track`
attribute moved to the label element) — it doesn't touch any open
blocker.

## Tomorrow's nudge
If the canonical memory file `project_needs_from_anthony_may13.md`
is checked in somewhere other than the workspace root, share the
path and tomorrow's diff will be run against that source instead of
the inferred status above.
