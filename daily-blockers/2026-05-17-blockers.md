# Morning Blockers — Sun 17 May 2026

Items only Anthony can action. Diffed against yesterday's list
(`2026-05-16-blockers.md`). **No commits landed overnight** — `git
log` shows `e4d24a49` (Fri 15 May) as still HEAD, so every code-side
assertion below is unchanged and re-verified against the same files.

> **Memory file note:** `project_needs_from_anthony_may13.md` is
> still not present in this workspace. Statuses below are inferred
> from the repo at HEAD. The uncommitted changes in the working tree
> are all blog `.mdx` edits and audit docs — none of them touch an
> open blocker. If the canonical memory file lives elsewhere, point
> me at it and I'll re-diff against that source instead.

---

## Active blockers (still need you)

### 1. Method course video scripts — STILL OUTSTANDING
- `src/lib/method/modules.ts` still has `videoYouTubeId: null` for
  **all 12 Roadman Method modules** (lines 66, 101, 138, 173, 208,
  248, 284, 319, 353, 388, 423, 459). Confirmed again at HEAD.
- The only scripted assets in `docs/` are still the 6 short-form
  videos: `teleprompter-scripts-13-may.md`, `teleprompter-clean.txt`,
  and `go-video-script.md`. No new module long-form scripts.
- **Decision needed:** which of the 12 modules
  (`01-where-you-actually-are` → `12-not-done-yet`) already have
  rough scripts vs. need writing, plus a target batch-record date.
  You said 1–2 days of batched recording is realistic — pick the
  date and we'll work backwards.

### 2. Beehiiv build for plateau sequences — STILL OUTSTANDING
- Copy still ready: `docs/plateau-diagnostic-email-sequences-v2.md`
  (4 sequences × 7 emails = 28 emails over 14 days, segmented by
  limiter tag `under-recovered` / `no-mans-land` / `strength-gap` /
  `fuelling-deficit`). `plateau-diagnostic-nurture-sequence.md` (v1,
  5 emails) is still in the repo root — assumption is v2 supersedes.
- Site-side wiring still correct: `/api/newsletter` calls
  `subscribeToBeehiiv` and forwards `source` as a tag (see
  `src/app/api/newsletter/route.ts` line 115 onwards — strips the
  leading slash, dedupes against `saturday-spin`).
- **Still outstanding inside Beehiiv:** build the four automations
  with the Day 0/+2/+4/+7/+9/+12/+14 cadence and the four limiter
  tags as triggers.
- **Decision needed:** confirm you or Sarah owns the Beehiiv build,
  and that v2 supersedes v1.

### 3. Exit intent + Masters Report Beehiiv wiring — STILL OUTSTANDING
- Code-side re-verified at HEAD:
  - `src/app/go/_components/GoExitIntent.tsx` line 154 posts to
    `/api/newsletter` with `source: "go-exit-intent"`.
  - `src/app/masters-report/_components/MastersReportCapture.tsx`
    line 45 builds source as `masters-report-${position}` and posts
    on line 67. TODO at lines 18–22 still calls out the missing
    Beehiiv segment/automation setup.
  - `src/app/api/newsletter/route.ts` matches both surfaces
    (`go-exit-intent` and `masters-report*`) to the
    `ImmediateAssetDeliverable` for the Resend PDF + Beehiiv tag
    bundle (lines 29–46).
- **What's still outstanding inside Beehiiv:** confirm the
  `go-exit-intent` and `masters-report` segments exist, and that
  the welcome / delivery automations on each are switched on.
- **Decision needed:** sign off on segment names + which
  automations should fire for each tag.

### 4. Method onboarding quiz — READY FOR YOUR FINAL READ
- Route is built and still ships behind `robots: { index: false,
  follow: false }` (page.tsx line 9). Client components
  (`OnboardingQuiz.tsx`, `QuizProgress.tsx`, `OptionCard.tsx`,
  `ResultsView.tsx`) and API at `src/app/api/method/onboarding/
  route.ts` are all in place. No changes overnight.
- Logic: 5 steps (goal, hours, level, FTP optional, target event
  optional), first three driving the recommendation in
  `recommend.ts` against the 48-cell matrix in `plan-matrix.ts`.
- **Decision needed:** read-through of `questions.ts` copy and the
  recommender mapping in `recommend.ts`. Once you green-light, flip
  the `robots` flag in `page.tsx` and start capturing onboarding
  completion data.

### 5. Stripe / Skool / Beehiiv API keys — STATUS UNCHANGED
- TrainingPeaks API access remains confirmed
  (`docs/trainingpeaks-plan-analysis.md`).
- `.env.example` still lists the same required slots: `BEEHIIV_API_KEY`,
  `BEEHIIV_PUBLICATION_ID`, `STRIPE_SECRET_KEY`,
  `STRIPE_WEBHOOK_SECRET`, `STRIPE_RACE_REPORT_PRICE_ID`,
  `SKOOL_EMAIL`, `SKOOL_PASSWORD`, `SKOOL_COMMUNITY_SLUG`.
- I still can't read prod `.env` from here. Same question as the
  last two mornings: which of these are populated in Vercel and
  which still need rotating / generating?

### 6. Test the Google Ads conversion — STILL OUTSTANDING
- Tag + conversion plumbing verified end-to-end again at HEAD:
  - `src/components/analytics/GoogleAdsTag.tsx` line 3 loads
    `AW-18123737652` via `Script strategy="afterInteractive"`.
  - `src/components/features/diagnostic/ResultsAnalytics.tsx`
    lines 15 + 39 fire
    `send_to: "AW-18123737652/WDZ_CNiOvKwcELSUicJD"` on diagnostic
    results view.
  - `src/app/go/ads/AdsLandingAnalytics.tsx` lines 15 + 37 fire
    `send_to: "AW-18123737652/up0JCJqHxKwcELSUicJD"` once per
    session on ads landing.
- **Action only you can take:** run one Plateau Diagnostic
  end-to-end on prod, then check Google Ads → Conversions for
  "Active" status on both labels (diagnostic complete + ads
  landing).

---

## Resolved / unchanged from yesterday

### TrainingPeaks legacy plan logic — RESOLVED (closed)
- Still closed. 222 workouts / 21 session types extracted across
  the 7 legacy plans; hour-band variants documented in
  `docs/trainingpeaks-hour-band-variants.md`. Building the 48 plan
  variants inside TrainingPeaks is the next *implementation* phase
  but it isn't a blocker on you.

---

## Diff vs. 16 May

| # | Item                                  | 16 May               | 17 May               |
|---|---------------------------------------|----------------------|----------------------|
| 1 | Method module video scripts           | PARTIAL (no change)  | PARTIAL (no change)  |
| 2 | Beehiiv build for plateau sequences   | UNRESOLVED           | UNRESOLVED           |
| 3 | Exit intent + Masters Report wiring   | UNRESOLVED           | UNRESOLVED           |
| 4 | Method onboarding quiz                | READY FOR FINAL READ | READY FOR FINAL READ |
| 5 | Stripe / Skool / Beehiiv API keys     | PARTIAL              | PARTIAL              |
| 6 | Google Ads conversion test            | UNRESOLVED           | UNRESOLVED           |
| 7 | TrainingPeaks legacy plan logic       | RESOLVED (closed)    | RESOLVED (closed)    |

Zero commits shipped in the last 24 hours, so the list is identical
to yesterday by construction. The uncommitted blog `.mdx` edits and
new audit docs in `git status` don't touch any open blocker.

## Suggested order if you can grab 30 minutes today
1. **Google Ads test** (10 min) — the cheapest unblock; one real
   diagnostic on prod and a glance at the Conversions tab.
2. **Onboarding quiz read-through** (15 min) — `questions.ts` +
   `recommend.ts`. Green-light here flips a live capture surface on.
3. **Pick the Beehiiv owner** (2 min) — you or Sarah on the v2
   plateau automations. Everything else (#2, #3) is waiting on this
   decision before anyone can move.

## Tomorrow's nudge
If `project_needs_from_anthony_may13.md` is checked in somewhere
other than the workspace root, share the path and tomorrow's diff
will run against that source instead of the inferred status above.
