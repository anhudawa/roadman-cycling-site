# Morning Blockers — Sat 23 May 2026

Diffed against `daily-blockers/2026-05-22-blockers.md`.

**Repo activity in the last 24 hours: zero.** HEAD is still
`b93b1c51` (21 May 08:23 BST, Meta Pixel dedupe). No commits,
no new docs, no new scripts. Working tree carries the same
blog `.mdx` edits and audit files as yesterday. Every blocker
on the 22 May list is in the same state today.

> `project_needs_from_anthony_may13.md` is still not in this
> workspace — re-searched again, the only `anthony*` matches
> are inside unrelated worktrees. Diff below is against the
> 22 May morning audit, which is the canonical source for the
> currently-open items.

---

## Closed since the scheduled-task list was written (unchanged)

| Original item | Status | Evidence |
|---|---|---|
| #2 Beehiiv plateau nurture sequences | ✅ LIVE | 4 automations running (Fuelling Deficit, Strength Gap, Polarisation Gap, Under-Recovered). 43–62% open, 51 enrolled. |
| #3 Exit intent + Masters Report Beehiiv wiring | ✅ SHIPPED | `/api/newsletter` handles `go-exit-intent` + `masters-report` with Beehiiv tag + Resend welcome + CRM upsert. |
| #5 Method onboarding quiz | ✅ SHIPPED | Live at `/method/onboarding`. 48-cell plan matrix, recommender, results view all built. |

Still 4 items needing you (plus the Google Ads / TrainingPeaks
splits called out below).

---

## Active blockers (still need Anthony)

### 1. Method course module video scripts — STILL OUTSTANDING (7th day)
- `src/lib/method/modules.ts` re-checked at HEAD: **12 of 13**
  modules still have `videoYouTubeId: null`. No change.
- No new module-length scripts have landed in `docs/` overnight.
- **Ask (7th time):** for each of the 12 outstanding modules
  (`01-where-you-actually-are` → `12-not-done-yet`) — rough
  script written, needs writing, or already filmed waiting on
  upload? Plus a target batch-record date.

### 2. Hero video for /go — STILL OUTSTANDING
- Script `docs/hero-video-script-60s.md` still sitting at the
  same length / mtime. No film, no upload.
- **Action only you can take:** record the 60-second take so
  the `/go` hero can be swapped.

### 3. Google Ads — three sub-items, all gated on 2FA
- **3a. Google Ads 2FA on `anthony@`** — still blocked. This
  is the single unblocker for 3b and 3c. **Highest-leverage
  5 minutes on the whole list.**
- **3b. Conversion test** — code unchanged at HEAD:
  `GoogleAdsTag.tsx:3` loads `AW-18123737652`;
  `ResultsAnalytics.tsx:15` fires `AW-18123737652/WDZ_CNiOvKwcELSUicJD`;
  `AdsLandingAnalytics.tsx:15` fires the `/go` label.
  **Seven days unverified.** Every day this sits, more ad
  spend goes unattributed.
- **3c. Ad groups 2–4** — can't be built until 3a is resolved.

### 4. TrainingPeaks legacy plan build — STILL BLOCKED
- Analysis done (222 workouts, 21 session types,
  `docs/trainingpeaks-hour-band-variants.md`).
- *Build* of the 48 variants inside TrainingPeaks still needs
  either access granted or a 30-min walkthrough.
- **Decision needed (same as yesterday):** grant TrainingPeaks
  access to whoever is building the variants, or put a 30-min
  walkthrough on the calendar.

### 5. CRM API keys — STATUS UNCHANGED (7th day)
- TrainingPeaks API access confirmed in
  `docs/trainingpeaks-plan-analysis.md`.
- `.env.example` slots unchanged: `BEEHIIV_API_KEY`,
  `BEEHIIV_PUBLICATION_ID`, `STRIPE_SECRET_KEY`,
  `STRIPE_WEBHOOK_SECRET`, `STRIPE_RACE_REPORT_PRICE_ID`,
  `SKOOL_EMAIL`, `SKOOL_PASSWORD`, `SKOOL_COMMUNITY_SLUG`.
- Same question as the last seven mornings: which of these
  are populated in Vercel, which still need rotating /
  generating?

---

## Resolved / not blocking you (unchanged from 22 May)

- Beehiiv plateau sequences — live, performing. v2 (3 → 7
  emails per sequence) in `docs/plateau-diagnostic-email-sequences-v2.md`
  is a future optimization, not a blocker.
- Exit intent + Masters Report wiring — shipped.
- Method onboarding quiz — shipped.
- Meta Pixel duplicate init — fixed in `b93b1c51`.
- Trustpilot free-plan compliance — fixed Tue in `9fddbef7`.

---

## Diff vs. 22 May morning

| # | Item | 22 May | 23 May |
|---|---|---|---|
| 1 | Method module video scripts | BLOCKED | BLOCKED (no change) |
| 2 | Hero video for /go | BLOCKED | BLOCKED (no change) |
| 3a | Google Ads 2FA | BLOCKED | BLOCKED (no change) |
| 3b | Google Ads conversion test | BLOCKED on 3a | BLOCKED on 3a |
| 3c | Google Ads ad groups 2–4 | BLOCKED on 3a | BLOCKED on 3a |
| 4 | TrainingPeaks 48-variant build | BLOCKED | BLOCKED (no change) |
| 5 | CRM API keys | BLOCKED | BLOCKED (no change) |

**Nothing moved.** Zero commits, zero new docs.

---

## Suggested order if you can grab 30 minutes this weekend

1. **Google Ads 2FA** (5 min) — single highest-leverage item.
   Unlocks 3b and 3c, stops a second week of unverified ad
   spend.
2. **Pick the TrainingPeaks unblock** (2 min) — grant access
   or put a 30-min walkthrough on the calendar. Either one
   moves the 48-variant build off your plate.
3. **Decide on the Method script status** (10 min) — even
   just labelling the 12 modules as "scripted / needs writing
   / filmed not uploaded" turns this from an open question
   into a workable backlog.
4. **Bonus 5 min:** open Vercel env, screenshot which CRM
   API slots are populated. Closes item 5 without you having
   to generate anything.

---

## Pattern worth flagging

This is the third consecutive morning with the same five
blockers and zero movement. Items 1, 4, 5 have now sat for
seven days; items 3a–c for six. None of them require more
than a few minutes from you to unblock further work — they
just need a decision or a credential. If a single 15-minute
block this weekend on 3a + 4 + 5 (the three quick-decision
items) is doable, that clears the path for the bulk of the
remaining engineering work next week.

---

## Tomorrow's nudge

If `project_needs_from_anthony_may13.md` lives somewhere
outside the workspace, share the path and the next diff will
run against the canonical source. If items 1, 2, 4, 5 have
been progressed outside the repo (Google Ads console,
TrainingPeaks, Vercel env), a one-liner closes them off the
list.
