# Morning Blockers — Tue 9 Jun 2026

Diffed against `daily-blockers/2026-06-08-blockers.md`.
Re-verified at HEAD `2c4c13ca` (29 May 14:24 BST).

**Zero commits in 257.6 hours (10.73 days).** HEAD has not
moved since the Friday 29 May transcription run. None of
B1–B5 have been touched. **Day 23.** Thirteenth consecutive
zero-Anthony-movement day. Yesterday's "Tuesday opens day 23
at ~257 hours / 10.7 days" prediction landed exactly.

Monday — the genuine movement window — passed untouched. As
flagged yesterday, the pattern now shifts from "weekend lull"
to a sustained working-week freeze: this is the first
confirmed weekday in the cycle to clear without action.

> `project_needs_from_anthony_may13.md` searched again
> (`find -iname 'project_needs*'` across the whole
> filesystem). Still no match after twenty-three mornings.
> The original scheduled-task list and the 24 May rollup
> remain canonical.

---

## What moved overnight (Anthony-relevant: nothing)

Zero commits since Friday 29 May 14:24 BST
(`git log --since "2026-05-29 15:00"` → 0). The freeze now
stands at 257.6 hours — a full working week, a complete
weekend, and the first day of a third working week, all
uncrossed.

The only working-tree churn is machine output, not Anthony
action: untracked back-catalogue `.mdx` transcripts under
`content/podcast/`, the untracked `PDF-GROUNDING-AUDIT.md`,
plus the usual `.claude/` lock/settings churn and
`scripts/seo-batch.ts` / `transcribe-audio-state.json`
deltas. None of it touches a blocker.

**Re-verified at HEAD this morning (all identical to
30 May → 8 Jun):**

- `src/lib/method/modules.ts` — **12 of 12** modules with
  `videoYouTubeId: null`.
- `src/app/go/page.tsx` — `HeroVideo` **not imported**
  (`grep -c HeroVideo src/app/go/page.tsx` returns 0);
  still exported from `src/components/ui/index.ts`.
- `docs/method-module-teleprompter-scripts.md` — still
  untracked, mtime still 24 May. **Sixteen days
  uncommitted**, same file, same 60–90s scope.
- `src/components/analytics/GoogleAdsTag.tsx` — only commit
  in its history is still `3066511e` (12 May).
  AW-18123737652 still loading, diagnostic and `/go` landing
  labels still unverified.
- `.env.example` — same BEEHIIV / STRIPE / RESEND / SKOOL
  slots, unchanged.

---

## Cross-off against the 7-item scheduled-task list

Already resolved (carried forward, re-verified, unchanged):

| # | Original item | Status | Evidence |
|---|---|---|---|
| ~~2~~ | ~~Beehiiv post-diagnostic nurture sequences~~ | LIVE | 4 automations running; `src/app/api/newsletter/route.ts` still matches `go-exit-intent` + `masters-report` (12 hits). |
| ~~3~~ | ~~Exit intent + Masters Report Beehiiv wiring~~ | SHIPPED | Newsletter route handles both form endpoints, re-verified. |
| ~~5~~ | ~~Method onboarding quiz~~ | SHIPPED | Route live at `src/app/(method)/method/onboarding/page.tsx`; `plan-matrix.ts` present. |

Items **1, 4, 6, 7** remain open, **day 23**. Carried-over
extras: Google Ads 2FA + ad groups 2–4 (split out of #7),
and the `/go` hero video swap (adjacent to #1).

---

## Active blockers (still need Anthony)

### B1. Method course module video scripts — day 23, scope still ambiguous

- 12 of 12 module entries still have `videoYouTubeId: null`.
- `docs/method-module-teleprompter-scripts.md` still
  untracked, **sixteen days** uncommitted, still 60–90s per
  module — not the 10–15 min the original brief called for.
- Three interpretations still open:
  1. Scope shifted to short intro/companion videos (file is
     the deliverable, just confirm).
  2. Hook-only; the 10–15 min module bodies still need
     writing.
  3. Short video as entry point + long-form MDX underneath.
- **Ask (unchanged):** one Slack reply naming (1), (2), or
  (3). If (1) or (3), pick a batch-record date.

### B2. Hero video for /go — code shelf-ready, recording on you

- `HeroVideo` still exported from `src/components/ui/index.ts`.
  Renders the designed placeholder while `videoYouTubeId`
  is `null`.
- **Re-verified:** still not imported into
  `src/app/go/page.tsx`. Two-line change for Ted the moment
  an unlisted YouTube ID exists.
- **Action only you can take:** record the 60-second take.
  Pairs naturally with B1's batch day.

### B3. Google Ads — three sub-items, all still gated on 2FA (day 23)

- **B3a. Google Ads 2FA on `anthony@`** — still blocked.
  Five-minute action. **Twenty-three days running.** Single
  highest-leverage item on the list.
- **B3b. Conversion test** — `GoogleAdsTag.tsx` still on
  commit `3066511e`; diagnostic + `/go` labels wired but
  unverified for twenty-three days.
- **B3c. Ad groups 2–4** — still blocked behind B3a.
- **Adjacent code fixes (Ted tasks once you prioritise):**
  - `?source=ads` from the ad landing never reaches Beehiiv
    / CRM because the diagnostic reads `?utm_source`.
  - Exit-intent popup currently fires on top of the live
    quiz on `/plateau`.

### B4. TrainingPeaks legacy plan build — day 23, status unchanged

- Analysis done: 222 workouts, 21 session types,
  `docs/trainingpeaks-hour-band-variants.md`, 48-cell matrix
  in `src/lib/method/onboarding/plan-matrix.ts`.
- Build of the 48 variants inside TrainingPeaks still needs
  either access granted or a 30-min walkthrough.
- **Decision (same as the last nineteen mornings):** grant
  TrainingPeaks access to whoever is building the variants,
  or put a 30-min walkthrough on the calendar.

### B5. CRM / external API keys — day 23, status unchanged

- `.env.example` slots unchanged: `BEEHIIV_API_KEY`,
  `BEEHIIV_PUBLICATION_ID`, `STRIPE_SECRET_KEY`,
  `STRIPE_STRENGTH_PRICE_ID`, `STRIPE_RACE_REPORT_PRICE_ID`,
  `STRIPE_WEBHOOK_SECRET`, `RESEND_API_KEY`, `SKOOL_EMAIL`,
  `SKOOL_PASSWORD`, `SKOOL_COMMUNITY_SLUG`,
  `SKOOL_INTRO_CATEGORY`.
- Same question as the last twenty-three mornings: which of
  these are populated in Vercel, which still need rotating /
  generating?

---

## Today's smallest unblocking moves, ranked by leverage

1. **Google Ads 2FA (B3a)** — still the single
   highest-leverage five minutes on the list. **Twenty-three
   days open.** Phone-in-hand task. Clears B3b, B3c, and
   converts the funnel-audit findings into measurable action.
2. **`.env` audit (B5)** — five minutes in Vercel. The other
   sub-ten-minute clear.
3. **Confirm the Method video scope (B1)** — one Slack reply.
   Picks the shape of the next two weeks.
4. **Record the /go hero video (B2)** — component built;
   you're the only blocker. Pair with the B1 batch day.
5. **TrainingPeaks access or walkthrough (B4)** — pick one,
   30 minutes; a weekday calendar item — and today is a
   weekday.

---

## Pattern note

Thirteen consecutive days of zero Anthony-relevant movement
(27 May → 9 Jun inclusive). The repo has been frozen for
257.6 hours straight — a full working week, a complete
weekend, and now into the first weekday of a third week.

Yesterday's open question — whether the freeze was a
"weekend lull" or a sustained working-week freeze — is
answered: **Monday passed untouched.** The cheapest path
forward is unchanged in shape but now overdue: a single
phone-and-laptop pass clears B3a + B5 in under ten minutes,
plus one Slack line on B1 scope. Compounding costs unchanged:
a third week of unmeasured Google Ads spend, an unscoped
Method recording backlog, and Ted still building defensively
around unknown env state.

Same suggested carve-outs (still the right shape):

- **10 min today:** Google Ads 2FA + `.env` audit
  (B3a + B5). Both clear with phone in hand.
- **One Slack message:** Method video scope decision (B1).
  Picks the shape of the next two weeks.
- **Half-day this week, blocked on calendar:** TrainingPeaks
  variant build or walkthrough (B4).
- **Recording day this week, paired with B1:** module videos
  (if B1 lands on option 1 or 3) + the /go hero (B2). One
  studio setup, two deliverables.

---

## Notes for tomorrow's diff

- If today (Tuesday) passes untouched, Wednesday's report
  opens day 24 at ~281 hours / 11.7 days.
- Watch whether `HeroVideo` gets imported into
  `src/app/go/page.tsx` and whether `videoYouTubeId` ever
  flips off `null`.
- Watch whether `docs/method-module-teleprompter-scripts.md`
  finally gets committed — sixteen days untracked.
- If B3a clears, expect a flurry of follow-up code changes
  (UTM normalisation, exit-popup suppression on `/plateau`)
  to surface in the next diff.
- With Monday gone untouched, the working-week-freeze read
  is now the base case. Any midweek commit would be the
  first weekday movement in the cycle.
