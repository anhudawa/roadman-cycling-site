# Morning Blockers — Wed 27 May 2026

Diffed against `daily-blockers/2026-05-26-blockers.md`.

**Repo activity in the last 24 hours: 28 commits, but zero on the
Anthony-blocker surface.** HEAD has moved from `5c807d62`
(25 May 19:14 BST) to `f740421f` (26 May 17:10 BST). Ted spent
the day on content infrastructure — back-catalogue transcription
pipeline, structured claim/quote extraction, answer-page
expansion to 254 across 12 clusters, expert-topic system, the
Unbound 2026 guide, a sitewide slop audit (45 fixes across 38
posts), and the `expert_quote` citation wiring into Ask
Roadman. None of it touches the seven items on the original
scheduled-task list.

> `project_needs_from_anthony_may13.md` searched again
> (`find -iname`, `git log --all -- '*may13*'`,
> `grep -r project_needs`). Still no match anywhere in the
> tree or git history. The original scheduled-task list and
> the 24 May rollup remain the canonical sources.

---

## What moved overnight (Anthony-relevant: nothing)

The 28 commits split into three buckets, none of which clears
a blocker:

| Bucket | Representative commits | Blocker impact |
|---|---|---|
| Content infra | `1f751a1c` audio back-catalogue pipeline · `d1c7738a` + `f740421f` 69 new transcripts · `59066dca` structured claim/quote extraction · `bc54bb9f` Ask Roadman expert_quote citations | None — does not change the seven open items. |
| Programmatic SEO | `a10a8d0b` → `9a745faa` answer pages 168 → 254 · `0def3c95` expert-topic pages · `bf15065e` cluster index · `35960cba` event guides + training plans · `37ef507a` masters hub | None — adjacent to your Google Ads / funnel work but doesn't unblock B3a. |
| Editorial / hygiene | `abbdc965` slop audit (45 fixes) · `22d23e0b` Unbound rewrite · `adcfa561` Sarah-Sturm removal + CTA strip · `289c64f3` Hexus → Hexis typo cleanup | None. |

**Verified at HEAD this morning:**
- `src/lib/method/modules.ts` — **12 of 12** modules still
  have `videoYouTubeId: null`. No video URLs landed.
- `src/app/go/page.tsx` — `HeroVideo` component still **not
  imported** (`grep -rn HeroVideo src/app/go/` returns zero
  hits). Component remains on the shelf as of yesterday's
  `357f6e28`.
- `docs/method-module-teleprompter-scripts.md` — still
  untracked. Twelve 60–90s scripts, slug-aligned. The scope
  ambiguity from yesterday is unresolved.

---

## Cross-off against the 7-item scheduled-task list

Already resolved (no change since 24–25 May, carried forward):

| # | Original item | Status | Evidence |
|---|---|---|---|
| ~~2~~ | ~~Beehiiv post-diagnostic nurture sequences~~ | LIVE | 4 automations running (Fuelling Deficit, Strength Gap, Polarisation Gap, Under-Recovered). 43–62% open, 51 enrolled per 24 May note. |
| ~~3~~ | ~~Exit intent + Masters Report Beehiiv wiring~~ | SHIPPED | `/api/newsletter` handles `go-exit-intent` + `masters-report` with Beehiiv tag + Resend welcome + CRM upsert. |
| ~~5~~ | ~~Method onboarding quiz~~ | SHIPPED | Live at `/method/onboarding`. 48-cell plan matrix, recommender, results view all built. |

Items **1, 4, 6, 7** remain open, day 11. Carried-over
extras: Google Ads 2FA + ad groups 2–4 (split out of #7),
and the `/go` hero video swap (adjacent to #1).

---

## Active blockers (still need Anthony)

### B1. Method course module video scripts — day 11, scope still ambiguous

- 12 of 13 modules at HEAD have `videoYouTubeId: null`. The
  one exception is the type definition itself.
- `docs/method-module-teleprompter-scripts.md` is still
  untracked and still 60–90s per module, not the 10–15 min
  the original brief called for.
- Three interpretations open since yesterday — still your call:
  1. Scope shifted to short intro/companion videos (the file
     is the deliverable, just confirm).
  2. These are hook-only; the 10–15 min module bodies still
     need writing.
  3. Short video as the entry point + long-form MDX
     underneath.
- **Asks (unchanged):** confirm interpretation; if (1) or
  (3), pick a batch-record date and we slot the same
  dark-background, tight-frame setup as the diagnostic
  profile scripts.

### B2. Hero video for /go — code side still unblocked, recording still on you

- `HeroVideo` component (281 LOC, `src/components/ui/HeroVideo.tsx`)
  shipped yesterday and exported from
  `src/components/ui/index.ts`. Renders a designed placeholder
  while `videoYouTubeId` is `null`, lazy YouTube façade once
  populated.
- **Verified this morning:** still not imported into
  `src/app/go/page.tsx`. Two lines of work for Ted the
  moment an unlisted YouTube ID exists.
- Script `docs/hero-video-script-60s.md` unchanged (mtime
  19 May 07:33).
- **Action only you can take:** record the 60-second take.

### B3. Google Ads — three sub-items, all still gated on 2FA (day 11)

This is item #7 expanded. Yesterday's funnel audit
(`docs/google-ads-funnel-audit.md`) framed the urgency; nothing
in today's commits changes the picture.

- **B3a. Google Ads 2FA on `anthony@`** — still blocked.
  Single five-minute action that unblocks B3b and B3c.
- **B3b. Conversion test** — code unchanged at HEAD.
  `GoogleAdsTag.tsx` loads `AW-18123737652`;
  `ResultsAnalytics.tsx` fires the diagnostic conversion
  label; `AdsLandingAnalytics.tsx` fires the `/go` label.
  Eleven days unverified. Until B3a clears, the early-signal
  "Ads Landing Page View" stays in *Needs attention* and
  Google's bidding has nothing more frequent than the final
  1.4% action to optimise on.
- **B3c. Ad groups 2–4** — still blocked behind B3a.
- **Adjacent code-side fixes flagged in the funnel audit
  (Ted tasks once you prioritise):**
  - `?source=ads` from the ad landing never reaches Beehiiv
    / CRM because the diagnostic reads `?utm_source`.
    Internal attribution drops silently.
  - Exit-intent popup currently fires on top of the live
    quiz on `/plateau`.

### B4. TrainingPeaks legacy plan build — day 11, status unchanged

- Analysis done: 222 workouts, 21 session types,
  `docs/trainingpeaks-hour-band-variants.md`, 48-cell matrix
  in `docs/method-training-plan-matrix.md`.
- *Build* of the 48 variants inside TrainingPeaks still
  needs either access granted or a 30-min walkthrough.
- **Decision (same as the last seven mornings):** grant
  TrainingPeaks access to whoever is building the variants,
  or put a 30-min walkthrough on the calendar.

### B5. CRM / external API keys — day 11, status unchanged

- TrainingPeaks API access confirmed in
  `docs/trainingpeaks-plan-analysis.md`.
- `.env.example` slots unchanged: `BEEHIIV_API_KEY`,
  `BEEHIIV_PUBLICATION_ID`, `STRIPE_SECRET_KEY`,
  `STRIPE_WEBHOOK_SECRET`, `STRIPE_RACE_REPORT_PRICE_ID`,
  `RESEND_API_KEY`, `SKOOL_EMAIL`, `SKOOL_PASSWORD`,
  `SKOOL_COMMUNITY_SLUG`.
- Same question as the last eleven mornings: which of these
  are populated in Vercel, which still need
  rotating / generating?

---

## Today's smallest unblocking moves, ranked by leverage

1. **Google Ads 2FA (B3a)** — still the single
   highest-leverage five minutes on the list. Eleven days
   open. Unblocks B3b, B3c, and turns the funnel-audit
   findings from theory into measurable action.
2. **Confirm the Method video scope (B1)** — one Slack
   reply or one line in this thread. Determines whether B1
   becomes "schedule a record day" or "keep writing".
3. **Record the /go hero video (B2)** — component is built;
   you're the only blocker between placeholder and live
   video. Same dark-background setup as B1's batch day, so
   these naturally pair.
4. **TrainingPeaks access or walkthrough (B4)** — pick one,
   30 minutes.
5. **`.env` audit (B5)** — five minutes in Vercel; clears
   the longest-running ambiguity on the list.

---

## Notes for tomorrow's diff

- Watch whether `HeroVideo` gets imported into
  `src/app/go/page.tsx` and whether `videoYouTubeId` ever
  flips off `null` on either the hero or any module.
- Watch whether `docs/method-module-teleprompter-scripts.md`
  gets committed — still untracked after four days.
- If B3a clears, expect a flurry of follow-up code changes
  (UTM normalisation, exit-popup suppression on the quiz
  route) — these will surface in tomorrow's diff.
- The new structured claim/quote pipeline (`59066dca`,
  `bc54bb9f`) is unrelated to this list but worth flagging:
  it materially changes what Ask Roadman cites. If you want
  a quick QA pass on the new expert_quote surfaces, that's a
  separate ask.
