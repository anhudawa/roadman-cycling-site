# Morning Blockers — Fri 15 May 2026

Items only Anthony can action. Status reflects what was found in the repo
this morning vs. the original list from `project_needs_from_anthony_may13.md`.

> **Note on the memory file:** `project_needs_from_anthony_may13.md` was not
> found in the workspace this morning (no `memory/` dir surfaced). Status
> updates below are inferred from files committed in `docs/` and `src/`
> between 11–13 May. Treat as best-effort cross-reference, not source of
> truth. If the canonical memory file lives elsewhere, point me at it and
> I'll redo the diff tomorrow.

---

## Active blockers (still need you)

### 1. Method course video scripts — PARTIAL
- `docs/teleprompter-scripts-13-may.md` and `docs/go-video-script.md` cover
  6 scripts already: /go hero (45–60s), /go short cut (20s), and the four
  plateau profile videos (Under-Recovered, No-Man's-Land, Strength Gap,
  Fuelling Deficit).
- **Still outstanding:** the 12 Roadman Method course module videos
  (10–15 min each). The module manifest in `src/lib/method/modules.ts`
  still has `videoYouTubeId: null` placeholders for all 12 slots.
- **Decision needed from you:** which Method modules are already
  scripted vs. need writing, and a target batch-record date.

### 2. Beehiiv build for plateau sequences — UNRESOLVED (copy expanded)
- Copy has expanded since the original blocker was written. `docs/plateau-
  diagnostic-email-sequences-v2.md` now defines **4 sequences × 7 emails
  = 28 emails** over 14 days, segmented by limiter tag
  (`under-recovered`, `no-mans-land`, `strength-gap`, `fuelling-deficit`).
- **Still outstanding:** building those automations inside Beehiiv with
  the four tag triggers and the Day 0/+2/+4/+7/+9/+12/+14 cadence.
- **Decision needed from you:** confirm whether you or Sarah owns the
  Beehiiv build, and whether v2 supersedes the original v1 5-email
  sequence (assumption: yes).

### 3. Exit intent + Masters Report Beehiiv wiring — UNRESOLVED
- Components exist in code: `src/app/go/_components/GoExitIntent.tsx`,
  `src/components/features/conversion/ExitIntentPopup.tsx`, and the
  `/masters-report` page at `src/app/masters-report/`.
- Form posts route through `src/app/api/newsletter/route.ts`, but the
  Beehiiv publication ID / tag mapping for both surfaces still needs you
  to confirm the endpoint and the tags they should fire.
- **Decision needed from you:** confirm Beehiiv endpoint + tag for
  (a) /go exit intent submissions, (b) /masters-report squeeze form.

### 4. Final sign-off on Method onboarding quiz — MOSTLY DONE
- Code is in place under `src/lib/method/onboarding/` (`questions.ts`,
  `plan-matrix.ts`, `recommend.ts`, `types.ts`).
- 48-plan matrix is defined in `docs/method-training-plan-matrix.md`
  (4 goals × 4 hour-tiers × 3 levels), with edge-case tuning notes.
- **Decision needed from you:** final read-through of `questions.ts`
  and the recommender mapping in `recommend.ts` so we can ship the
  quiz at `/method/onboarding`.

### 5. Stripe / Skool / Beehiiv API keys — PARTIAL
- TrainingPeaks API access is confirmed and in use (account
  `tedcrilly@`, endpoint `tpapi.trainingpeaks.com/fitness/v6`, see
  `docs/trainingpeaks-plan-analysis.md`).
- Beehiiv, Skool, and Stripe integrations have admin surfaces
  (`src/app/admin/(dashboard)/settings/_components/IntegrationsPanel.tsx`,
  `src/app/admin/(dashboard)/integrations/skool/page.tsx`) but I can't
  verify from here whether the keys in `.env` are present/valid.
- **Decision needed from you:** confirm which keys are populated and
  which still need to be generated/shared.

### 6. Test the Google Ads conversion — UNRESOLVED
- Tag component lives at `src/components/analytics/GoogleAdsTag.tsx` and
  the conversion is wired through `src/lib/analytics/events.ts` plus the
  diagnostic results page (`ResultsAnalytics.tsx`) and ads landing
  (`AdsLandingAnalytics.tsx`).
- **Action only you can take:** once the code fix has shipped to
  production, complete one real Plateau Diagnostic end-to-end and check
  Google Ads → Conversions for "Active" status.

---

## Resolved since 13 May (no action needed)

### 7. TrainingPeaks legacy plan logic — RESOLVED
- All 7 of your legacy plans were extracted on 11 May (222 workouts,
  21 session types). See `docs/trainingpeaks-plan-analysis.md`.
- The hour-band gap is now documented: `docs/trainingpeaks-hour-band-
  variants.md` defines 6/8/10/12 hr variants and the day-pattern
  template (Mon REST, Tue Key1, Wed Endurance, Thu Key2, Fri REST,
  Sat Long, Sun REST).
- Building the 48 plan variants inside TrainingPeaks is the next phase
  (per the verification checklist in `method-training-plan-matrix.md`),
  but the **logic-extraction blocker** is closed.

---

## Tomorrow's nudge
If the canonical memory file is at a path other than the workspace root,
share it and I'll regenerate this diff against the real source of truth.
