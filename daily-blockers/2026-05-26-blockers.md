# Morning Blockers — Tue 26 May 2026

Diffed against `daily-blockers/2026-05-25-blockers.md`.

**Repo activity in the last 24 hours: six commits.** HEAD has
moved from `b93b1c51` (21 May 08:23 BST, Meta Pixel dedupe)
to `5c807d62` (25 May 19:14 BST, expert-quote fix). After
five mornings of zero movement, yesterday Ted shipped a
six-commit stack between 14:00 and 19:14 BST. The relevant
deltas for this list are summarised below.

> `project_needs_from_anthony_may13.md` still not in this
> workspace — searched again (`find`, `git log --all -- '*may13*'`,
> `grep -r project_needs`). No match. The 24 May morning
> report remains the canonical source for the currently-open
> items and itself traces back to the original 7-item
> scheduled-task list.

---

## What moved overnight

| Commit | Subject | Blocker impact |
|---|---|---|
| `357f6e28` | feat: add HeroVideo player for /go hero slot | Player component exists but **not yet wired into `/go`** (see B2). Component is plug-and-play, ready to accept a `videoYouTubeId`. |
| `71570e70` | docs: add Google Ads → conversion funnel audit | New 24KB audit at `docs/google-ads-funnel-audit.md`. Strong narrative on why CPA is €86 (funnel length + only the hardest action recording + UTM mismatch). Findings folded into B3 below. |
| `293f577d` | fix: remove all Trustpilot brand usage for C&D compliance | Compliance-only — no blocker impact. |
| `91555ccf` | fix: stop robots.txt blocking self-de-indexing pages | SEO hygiene — no blocker impact. |
| `341a57a9` | fix: correct broken URLs in llms.txt / llms-full.txt | SEO hygiene — no blocker impact. |
| `5c807d62` | fix: surface expert quotes on guest pages | Content-quality — no blocker impact. |

**Also new in the working tree (untracked, missed by yesterday's report):**
`docs/method-module-teleprompter-scripts.md` — 198 lines,
twelve scripts mapped 1-to-1 against the module slugs in
`src/lib/method/modules.ts`. **However**, each script is
**60–90 seconds**, not the **10–15 minutes** the original
scheduled-task brief called for. So this is either a
scope shift (short intros instead of full modules) or it
covers the hook portion only. Needs your call — see B1.

---

## Cross-off against the 7-item scheduled-task list

Already resolved (no change since 24–25 May):

| # | Original item | Status | Evidence |
|---|---|---|---|
| ~~2~~ | ~~Beehiiv post-diagnostic nurture sequences~~ | LIVE | 4 automations running (Fuelling Deficit, Strength Gap, Polarisation Gap, Under-Recovered). 43–62% open, 51 enrolled per 24 May note. |
| ~~3~~ | ~~Exit intent + Masters Report Beehiiv wiring~~ | SHIPPED | `/api/newsletter` handles `go-exit-intent` + `masters-report` with Beehiiv tag + Resend welcome + CRM upsert. |
| ~~5~~ | ~~Method onboarding quiz~~ | SHIPPED | Live at `/method/onboarding`. 48-cell plan matrix, recommender, results view all built. |

Items 1, 4, 6, 7 remain open. Carried-over extras: Google
Ads 2FA + ad groups 2–4 (split out of #7), and the `/go`
hero video swap (adjacent to #1).

---

## Active blockers (still need Anthony)

### B1. Method course module video scripts — STATUS SHIFT (10th day)

- `src/lib/method/modules.ts` re-checked at HEAD:
  **12 of 13** modules still have `videoYouTubeId: null`.
  No video URLs landed.
- **New:** `docs/method-module-teleprompter-scripts.md`
  (untracked, 24 May 07:14) — 12 scripts, slug-aligned,
  60–90s each.
- **Ambiguity to resolve:** original brief asked for
  **10–15 min** per module. The new file is **60–90s**.
  Three reasonable interpretations:
  1. Scope intentionally shifted to short
     intro/companion videos → great, just confirm.
  2. These are hook-only scripts; full module bodies
     still need writing → unchanged blocker.
  3. The modules will be short videos *plus* the
     long-form written protocol — i.e. video is the
     entry point, the MDX is the depth.
- **Asks:**
  - Which of the three above is the model?
  - If (1) or (3): batch-record date for all 12 short
    scripts. Same dark-background, tight-frame setup as
    the diagnostic profile scripts (per the file's
    delivery notes).
  - If (2): which of the 12 modules still need full
    script-writing?

### B2. Hero video for /go — UNBLOCKED ON THE CODE SIDE

- **New:** `HeroVideo` component shipped yesterday
  (`src/components/ui/HeroVideo.tsx`, 281 LOC). Renders
  a designed placeholder while `videoYouTubeId` is
  `null`, then a lazy YouTube façade once an ID is
  supplied. Exported from `src/components/ui/index.ts`.
- **Not yet imported into `src/app/go/page.tsx`** —
  verified with `grep -rn "HeroVideo" src/app/`.
  Component is sitting on the shelf.
- Script `docs/hero-video-script-60s.md` unchanged
  (1,579 bytes, mtime 19 May 07:33).
- **Action only you can take:** record the 60-second
  take. Once the unlisted YouTube ID exists, wiring it
  into `/go` is a one-line prop change Ted can do in
  ~2 minutes.

### B3. Google Ads — three sub-items, all still gated on 2FA

This is item #7 expanded into three sub-items. The new
funnel audit (`docs/google-ads-funnel-audit.md`) reframes
the urgency.

- **B3a. Google Ads 2FA on `anthony@`** — still blocked.
  Single unblocker for B3b and B3c.
- **B3b. Conversion test** — code unchanged at HEAD.
  `GoogleAdsTag.tsx` loads `AW-18123737652`;
  `ResultsAnalytics.tsx` fires the diagnostic
  conversion label; `AdsLandingAnalytics.tsx` fires the
  `/go` label. **Ten days unverified.** The funnel
  audit confirms only "Diagnostic Complete" is
  currently recording in console (10 of 737 clicks,
  1.4% — the survival rate of a 2-page + 18-screen
  quiz funnel). "Ads Landing Page View" is marked
  *Needs attention*. Until B3a clears, you cannot
  verify the early signal that lets Google's bidding
  optimise on something more frequent than the final
  action.
- **B3c. Ad groups 2–4** — still blocked behind B3a.
- **Adjacent findings from the funnel audit (no Anthony
  action needed yet, flagged for prioritisation):**
  - The ad-page `?source=ads` param never reaches
    Beehiiv / CRM because the diagnostic reads
    `?utm_source`. Internal attribution is being
    dropped silently. Code-side fix is a Ted task once
    you prioritise it.
  - Exit-intent popup currently fires *on top of the
    live quiz* on `/plateau`. Same — Ted task once
    you flag it.

### B4. TrainingPeaks legacy plan build — STATUS UNCHANGED (item #4)

- Analysis still done (222 workouts, 21 session types,
  `docs/trainingpeaks-hour-band-variants.md`, 48-cell
  matrix in `docs/method-training-plan-matrix.md`).
- *Build* of the 48 variants inside TrainingPeaks still
  needs either access granted or a 30-min walkthrough.
- **Decision needed (same as the last six mornings):**
  grant TrainingPeaks access to whoever is building the
  variants, or put a 30-min walkthrough on the calendar.

### B5. CRM / external API keys — STATUS UNCHANGED (10th day, item #6)

- TrainingPeaks API access confirmed in
  `docs/trainingpeaks-plan-analysis.md`.
- `.env.example` slots unchanged: `BEEHIIV_API_KEY`,
  `BEEHIIV_PUBLICATION_ID`, `STRIPE_SECRET_KEY`,
  `STRIPE_WEBHOOK_SECRET`, `STRIPE_RACE_REPORT_PRICE_ID`,
  `RESEND_API_KEY`, `SKOOL_EMAIL`, `SKOOL_PASSWORD`,
  `SKOOL_COMMUNITY_SLUG`.
- Same question as the last ten mornings: which of
  these are populated in Vercel, which still need
  rotating / generating?

---

## Today's smallest unblocking moves, ranked by leverage

1. **Google Ads 2FA (B3a)** — still the single highest-leverage
   five minutes on the list. Unblocks B3b and B3c and turns
   the funnel-audit findings from theory into measurable
   action.
2. **Confirm the Method video scope (B1)** — one Slack
   reply answers whether the 12 teleprompter scripts are
   the deliverable or just the intros. Determines whether
   B1 is now "schedule a record day" or "keep writing".
3. **Record the /go hero video (B2)** — component is built;
   you're the only blocker between placeholder and live
   video.
4. **TrainingPeaks access or walkthrough (B4)** — pick one,
   30 minutes.
5. **`.env` audit (B5)** — five minutes in Vercel; clears
   the longest-running ambiguity on the list.

---

## Notes for tomorrow's diff

- Watch whether the HeroVideo component gets wired into
  `/go/page.tsx` and whether `videoYouTubeId` ever
  flips off `null` (on either the hero or any module).
- Watch whether the teleprompter scripts file gets
  committed — currently untracked.
- If B3a clears, expect a flurry of follow-up code
  changes (UTM normalisation, exit-popup suppression on
  the quiz route) — these will surface in tomorrow's diff.
