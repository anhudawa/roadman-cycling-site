# Morning Blockers — Tue 19 May 2026

Items only Anthony can action. Diffed against yesterday's list
(`2026-05-18-blockers.md`). **Still zero commits since Fri 15 May
18:02 BST** — `git log -1` is unchanged at `e4d24a49`. This is now
the **fourth consecutive identical morning** (Sat 16 → Tue 19).
Working-tree changes are still the blog `.mdx` edits, new audit
docs and the `daily-blockers/` folder — none of them touch an open
blocker.

> **Memory file note:** `project_needs_from_anthony_may13.md` is
> still not in this workspace — re-searched today, no match.
> Statuses below are inferred from repo state at HEAD. If the
> canonical memory file lives elsewhere, share the path and the
> next diff will run against that source.

---

## Active blockers (still need you)

### 1. Method course video scripts — STILL OUTSTANDING (4th day)
- `src/lib/method/modules.ts` still has `videoYouTubeId: null` for
  **12 of 13 module entries** (re-counted today). All 12 Roadman
  Method modules unchanged.
- Only scripted long-form assets in `docs/` remain the short-form
  set: `teleprompter-scripts-13-may.md`, `teleprompter-clean.txt`,
  `go-video-script.md`. No new module scripts added overnight.
- **Decision needed (4th ask):** which of the 12 modules
  (`01-where-you-actually-are` → `12-not-done-yet`) have rough
  scripts vs. need writing, plus a target batch-record date.

### 2. Beehiiv build for plateau sequences — STILL OUTSTANDING (4th day)
- Copy still ready: `docs/plateau-diagnostic-email-sequences-v2.md`
  (4 sequences × 7 emails = 28 emails over 14 days, segmented by
  limiter tag `under-recovered` / `no-mans-land` / `strength-gap` /
  `fuelling-deficit`). v1 file `plateau-diagnostic-nurture-sequence.md`
  still sits in repo root — assumption is v2 supersedes.
- Note: a new audit doc `docs/plateau-diagnostic-email-audit.md`
  landed on 18 May (untracked in git, 47 KB). Doesn't change the
  Beehiiv build status — copy was already ready; the bottleneck is
  the build itself in Beehiiv.
- Site-side wiring still correct: `/api/newsletter` calls
  `subscribeToBeehiiv` and forwards `source` as a tag.
- **Decision needed:** confirm you or Sarah owns the Beehiiv build,
  and confirm v2 supersedes v1.

### 3. Exit intent + Masters Report Beehiiv wiring — STILL OUTSTANDING (4th day)
- Code-side re-verified at HEAD, unchanged:
  - `src/app/go/_components/GoExitIntent.tsx:154` still posts
    `source: "go-exit-intent"` to `/api/newsletter`.
  - `src/app/masters-report/_components/MastersReportCapture.tsx:45`
    still builds `source = masters-report-${position}`. TODO at
    line 18 still flags the missing Beehiiv segment/automation.
  - `src/app/api/newsletter/route.ts:29-43` matches both surfaces
    and routes them to `ImmediateAssetDeliverable` (Resend PDF +
    Beehiiv tag bundle).
- **What's outstanding inside Beehiiv:** confirm `go-exit-intent`
  and `masters-report` segments exist, and the welcome / delivery
  automations on each are switched on.
- **Decision needed:** sign off on segment names + which
  automations fire for each tag.

### 4. Method onboarding quiz — READY FOR YOUR FINAL READ (4th day)
- Route still ships behind `robots: { index: false, follow: false }`
  (`src/app/(method)/method/onboarding/page.tsx:10`, re-verified
  today). Client components and `src/app/api/method/onboarding/route.ts`
  all in place.
- Logic: 5 steps (goal, hours, level, FTP optional, target event
  optional) → `questions.ts` → `recommend.ts` against the 48-cell
  matrix in `plan-matrix.ts`.
- **Decision needed:** read-through of `questions.ts` copy and
  `recommend.ts` mapping. Green-light flips the `robots` flag
  and turns on onboarding completion capture.

### 5. Stripe / Skool / Beehiiv API keys — STATUS UNCHANGED (4th day)
- TrainingPeaks API access remains confirmed
  (`docs/trainingpeaks-plan-analysis.md`).
- `.env.example` slots unchanged: `BEEHIIV_API_KEY`,
  `BEEHIIV_PUBLICATION_ID`, `STRIPE_SECRET_KEY`,
  `STRIPE_WEBHOOK_SECRET`, `STRIPE_RACE_REPORT_PRICE_ID`,
  `SKOOL_EMAIL`, `SKOOL_PASSWORD`, `SKOOL_COMMUNITY_SLUG`.
- I can't read prod `.env` from here. Same question as the last
  four mornings: which of these are populated in Vercel, which
  still need rotating / generating?

### 6. Test the Google Ads conversion — STILL OUTSTANDING (4th day)
- Tag + conversion plumbing verified again at HEAD:
  - `src/components/analytics/GoogleAdsTag.tsx:3` loads
    `AW-18123737652`.
  - `src/components/features/diagnostic/ResultsAnalytics.tsx:15`
    fires `AW-18123737652/WDZ_CNiOvKwcELSUicJD` on diagnostic
    results view.
  - `src/app/go/ads/AdsLandingAnalytics.tsx:15` fires
    `AW-18123737652/up0JCJqHxKwcELSUicJD` once per session on
    ads landing.
- **Action only you can take:** run one Plateau Diagnostic
  end-to-end on prod, then check Google Ads → Conversions for
  "Active" status on both labels. Four days unverified — if
  attribution is broken you're paying for clicks you can't measure.

---

## Resolved / unchanged

### TrainingPeaks legacy plan logic — RESOLVED (still closed)
- The scheduled-task email lists this as item #4, but it was closed
  out earlier this month. 222 workouts / 21 session types extracted
  across the 7 legacy plans; hour-band variants documented in
  `docs/trainingpeaks-hour-band-variants.md`. Building the 48 plan
  variants inside TrainingPeaks is the next *implementation* phase
  but no longer blocked on you.

---

## Diff vs. 18 May

| # | Item                                  | 18 May               | 19 May               |
|---|---------------------------------------|----------------------|----------------------|
| 1 | Method module video scripts           | PARTIAL (no change)  | PARTIAL (no change)  |
| 2 | Beehiiv build for plateau sequences   | UNRESOLVED           | UNRESOLVED           |
| 3 | Exit intent + Masters Report wiring   | UNRESOLVED           | UNRESOLVED           |
| 4 | Method onboarding quiz                | READY FOR FINAL READ | READY FOR FINAL READ |
| 5 | Stripe / Skool / Beehiiv API keys     | PARTIAL              | PARTIAL              |
| 6 | Google Ads conversion test            | UNRESOLVED           | UNRESOLVED           |
| 7 | TrainingPeaks legacy plan logic       | RESOLVED (closed)    | RESOLVED (closed)    |

Four consecutive mornings of the same six asks. HEAD has been
`e4d24a49` since Fri 15 May 18:02 BST — 88+ hours without a commit.
Worth flagging *again* in case any of these were silently handled
outside the repo (Beehiiv, Vercel, Google Ads) and the status here
is just stale.

## Suggested order if you can grab 30 minutes today
1. **Google Ads test** (10 min) — cheapest unblock. One real
   diagnostic on prod, glance at the Conversions tab. Four days
   unverified is the riskiest item on the list.
2. **Onboarding quiz read-through** (15 min) — `questions.ts` +
   `recommend.ts`. Green-light flips a live capture surface on.
3. **Pick the Beehiiv owner** (2 min) — you or Sarah on the v2
   plateau automations. Items #2 and #3 both wait on this
   decision before anyone can move.

## Tomorrow's nudge
If `project_needs_from_anthony_may13.md` is checked in somewhere
other than the workspace root, share the path and the next diff
will run against that source instead of inferred status. If items
#2, #3 or #5 *were* handled inside Beehiiv / Vercel / Google Ads
this weekend, a one-liner closes them off the list.
