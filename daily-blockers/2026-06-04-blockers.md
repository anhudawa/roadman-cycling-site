# Morning Blockers — Thu 4 Jun 2026

Diffed against `daily-blockers/2026-06-03-blockers.md`.
Re-verified at HEAD `2c4c13ca` (29 May 14:24 BST).

**Zero commits in 138 hours.** HEAD has not moved since
the Friday afternoon transcription run. None of B1–B5
have been touched. **Day 18.** Eighth consecutive
zero-Anthony-movement day; the freeze has now spanned a
full working week of inaction. Yesterday's "137 hours
tomorrow morning" prediction landed within an hour.

> `project_needs_from_anthony_may13.md` searched again
> (`find -iname '*may13*'`, `grep -rn project_needs`).
> Still no match after eighteen mornings. The original
> scheduled-task list and the 24 May rollup remain
> canonical.

---

## What moved overnight (Anthony-relevant: nothing)

Zero commits since Friday 29 May 14:24 BST. The 138-hour
freeze is now the longest gap in the eighteen-day window
by a clear margin and crosses the full-working-week line.

**Re-verified at HEAD this morning (all identical to
30 May → 3 Jun):**

- `src/lib/method/modules.ts` — **12 of 12** modules with
  `videoYouTubeId: null`.
- `src/app/go/page.tsx` — `HeroVideo` **not imported**
  (`grep -n HeroVideo src/app/go/page.tsx` returns zero).
- `docs/method-module-teleprompter-scripts.md` — still
  untracked. Mtime still 24 May 07:14. **Eleven days
  uncommitted**, same 198-line file, same 60–90s scope.
- `docs/hero-video-script-60s.md` — unchanged since 19 May.
- `src/components/analytics/GoogleAdsTag.tsx` — only
  commit in its history is still `3066511e` (12 May).
  AW-18123737652 still loading, diagnostic and `/go`
  landing labels still unverified.
- `.env.example` — 11 BEEHIIV/STRIPE/RESEND/SKOOL slots,
  unchanged.

---

## Cross-off against the 7-item scheduled-task list

Already resolved (carried forward, unchanged):

| # | Original item | Status | Evidence |
|---|---|---|---|
| ~~2~~ | ~~Beehiiv post-diagnostic nurture sequences~~ | LIVE | 4 automations running (Fuelling Deficit, Strength Gap, Polarisation Gap, Under-Recovered). |
| ~~3~~ | ~~Exit intent + Masters Report Beehiiv wiring~~ | SHIPPED | `src/app/api/newsletter/route.ts` handles `go-exit-intent` + `masters-report` with Beehiiv tag + Resend welcome + CRM upsert. |
| ~~5~~ | ~~Method onboarding quiz~~ | SHIPPED | Route live at `src/app/(method)/method/onboarding/page.tsx`. Plan matrix, questions, recommender, results view all in `src/lib/method/onboarding/*.ts`. |

Items **1, 4, 6, 7** remain open, **day 18**. Carried-over
extras: Google Ads 2FA + ad groups 2–4 (split out of #7),
and the `/go` hero video swap (adjacent to #1).

---

## Active blockers (still need Anthony)

### B1. Method course module video scripts — day 18, scope still ambiguous

- 12 of 12 module entries still have `videoYouTubeId: null`.
- `docs/method-module-teleprompter-scripts.md` still
  untracked, **eleven days** uncommitted, still 60–90s per
  module — not the 10–15 min the original brief called for.
- Three interpretations still open:
  1. Scope shifted to short intro/companion videos (file is
     the deliverable, just confirm).
  2. Hook-only; the 10–15 min module bodies still need
     writing.
  3. Short video as entry point + long-form MDX underneath.
- **Ask (unchanged):** one Slack reply naming (1), (2),
  or (3). If (1) or (3), pick a batch-record date.

### B2. Hero video for /go — code shelf-ready, recording on you

- `HeroVideo` component still exported from
  `src/components/ui/index.ts`. Renders the designed
  placeholder while `videoYouTubeId` is `null`.
- **Re-verified:** still not imported into
  `src/app/go/page.tsx`. Two-line change for Ted the
  moment an unlisted YouTube ID exists.
- **Action only you can take:** record the 60-second take.
  Pairs naturally with B1's batch day.

### B3. Google Ads — three sub-items, all still gated on 2FA (day 18)

- **B3a. Google Ads 2FA on `anthony@`** — still blocked.
  Five-minute action. **Eighteen days running.** Single
  highest-leverage item on the list.
- **B3b. Conversion test** — `GoogleAdsTag.tsx` still on
  commit `3066511e`; diagnostic + `/go` labels wired but
  unverified for eighteen days.
- **B3c. Ad groups 2–4** — still blocked behind B3a.
- **Adjacent code fixes (Ted tasks once you prioritise):**
  - `?source=ads` from the ad landing never reaches Beehiiv
    / CRM because the diagnostic reads `?utm_source`.
  - Exit-intent popup currently fires on top of the live
    quiz on `/plateau`.

### B4. TrainingPeaks legacy plan build — day 18, status unchanged

- Analysis done: 222 workouts, 21 session types,
  `docs/trainingpeaks-hour-band-variants.md`, 48-cell
  matrix in `src/lib/method/onboarding/plan-matrix.ts`.
- Build of the 48 variants inside TrainingPeaks still
  needs either access granted or a 30-min walkthrough.
- **Decision (same as the last fourteen mornings):** grant
  TrainingPeaks access to whoever is building the
  variants, or put a 30-min walkthrough on the calendar.

### B5. CRM / external API keys — day 18, status unchanged

- `.env.example` slots unchanged: `BEEHIIV_API_KEY`,
  `BEEHIIV_PUBLICATION_ID`, `STRIPE_SECRET_KEY`,
  `STRIPE_WEBHOOK_SECRET`, `STRIPE_RACE_REPORT_PRICE_ID`,
  `RESEND_API_KEY`, `SKOOL_EMAIL`, `SKOOL_PASSWORD`,
  `SKOOL_COMMUNITY_SLUG`.
- Same question as the last eighteen mornings: which of
  these are populated in Vercel, which still need
  rotating / generating?

---

## Today's smallest unblocking moves, ranked by leverage

1. **Google Ads 2FA (B3a)** — still the single
   highest-leverage five minutes on the list.
   **Eighteen days open.** Clears B3b, B3c, and converts
   the funnel-audit findings into measurable action.
2. **Confirm the Method video scope (B1)** — one Slack
   reply. Picks the shape of the next two weeks.
3. **Record the /go hero video (B2)** — component built;
   you're the only blocker. Pair with the B1 batch day.
4. **TrainingPeaks access or walkthrough (B4)** — pick
   one, 30 minutes.
5. **`.env` audit (B5)** — five minutes in Vercel.

---

## Pattern note

Eight consecutive days of zero Anthony-relevant movement
(27 → 28 → 29 → 30 → 31 May → 1 → 2 → 3 Jun). The repo
has now been frozen for 138 hours straight, crossing the
full-working-week line: Friday afternoon → Thursday
morning with no commits. Yesterday's prediction
("137 hours tomorrow") landed within an hour.

The two 5-minute items (B3a Google Ads 2FA, B5 Vercel
env audit) remain the cheapest unblocking moves in the
backlog and are now both eighteen days old. Compounding
costs this week: a full week of unmeasured Google Ads
spend, a full week of unscoped Method recording, a full
week Ted has been building defensively around unknown
env state.

Same suggested carve-outs (still the right shape):

- **5 min today, before your next thing:** Google Ads 2FA
  + `.env` audit (B3a + B5). Both clear with phone in hand.
- **One Slack message today:** Method video scope decision
  (B1). Picks the shape of the next two weeks.
- **Half-day this week, blocked on calendar:** TrainingPeaks
  variant build or walkthrough (B4).
- **Recording day this week, paired with B1:** module
  videos (if B1 lands on option 1 or 3) + the /go hero
  (B2). One studio setup, two deliverables.

---

## Notes for tomorrow's diff

- Watch whether `HeroVideo` gets imported into
  `src/app/go/page.tsx` and whether `videoYouTubeId` ever
  flips off `null`.
- Watch whether `docs/method-module-teleprompter-scripts.md`
  finally gets committed — eleven days untracked.
- If B3a clears, expect a flurry of follow-up code changes
  (UTM normalisation, exit-popup suppression on `/plateau`)
  to surface in the next diff.
- If HEAD is still at `2c4c13ca` tomorrow morning, the
  freeze will be at 162 hours / 6.75 days and will span
  a full working week **plus the start of a second**. At
  that point the five-minute items (B3a, B5) will be
  visibly out of proportion to their cost: eighteen days
  for a phone tap and a Vercel scroll.
