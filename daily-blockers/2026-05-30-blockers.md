# Morning Blockers — Sat 30 May 2026

Diffed against `daily-blockers/2026-05-29-blockers.md`.

**Eleven commits in the last 24 hours, none Anthony-relevant.**
HEAD moved from `7a16baca` (28 May 23:10 BST) to
`2c4c13ca` (29 May 14:24 BST). All eleven are SEO,
back-catalogue transcription, knowledge-graph, and
Ask-Roadman lead-gen work; none touch the module video,
hero video, Google Ads, TrainingPeaks, or CRM surfaces.

> `project_needs_from_anthony_may13.md` searched again
> (`find -iname '*may13*'`, `grep -rn project_needs`).
> Still no match after fourteen mornings. The original
> scheduled-task list and the 24 May rollup remain
> canonical.

---

## What moved overnight (Anthony-relevant: nothing)

Eleven commits landed, all by Ted, all unrelated to the
open blocker list:

| SHA | Subject | Surface |
|---|---|---|
| `2c4c13ca` | content: transcribe 100 more audio-only back-catalogue episodes | podcast transcripts |
| `784c0406` | content: transcribe 100 more audio-only back-catalogue episodes | podcast transcripts |
| `f924887d` | chore(kg): canonical entities + recent-episode coverage | knowledge graph |
| `97364e58` | fix(seo): repoint 7 invented answer-slugs at real ones | answers route |
| `2df92b7a` | seo: internal linking sweep on 20 newest blog posts | blog |
| `9f49c13e` | content: add 5 masters cycling answer pages | answers route |
| `9281427c` | fix(seo): sitemap orphan, guest dup slugs, glossary dup, /about @id mismatch | sitemap + entities |
| `b91e4658` | feat: email magic-link auth gate for Ask Roadman (lead gen → Beehiiv) | Ask Roadman |
| `7c9153d4` | ask: add corrections override + QA grounding test suite | Ask Roadman QA |
| `ceb6d4e7` | revert: restore 65K+ subscriber count in MCP seed, beehiiv comment, camp email | seed copy |
| `d9af01f2` | revert: restore 65K+ newsletter subscriber count (matches pitch decks) | seed copy |

**Re-verified at HEAD this morning (all identical to 29 May):**
- `src/lib/method/modules.ts` — **12 of 12** modules with
  `videoYouTubeId: null`.
- `src/app/go/page.tsx` — `HeroVideo` **not imported**
  (`grep -n HeroVideo src/app/go/page.tsx` returns zero).
- `docs/method-module-teleprompter-scripts.md` — still
  untracked (`git status` shows `??`). Same 60–90s
  scripts, same scope ambiguity. Now seven days
  uncommitted.
- `docs/hero-video-script-60s.md` — unchanged
  (mtime 19 May 07:33).
- `src/components/analytics/GoogleAdsTag.tsx` — only
  commit in its history is still the original
  `3066511e`. Still loading `AW-18123737652` with the
  diagnostic and ad-landing labels wired but unverified.

---

## Cross-off against the 7-item scheduled-task list

Already resolved (carried forward, unchanged):

| # | Original item | Status | Evidence |
|---|---|---|---|
| ~~2~~ | ~~Beehiiv post-diagnostic nurture sequences~~ | LIVE | 4 automations running (Fuelling Deficit, Strength Gap, Polarisation Gap, Under-Recovered). 43–62% open, 51 enrolled per 24 May note. |
| ~~3~~ | ~~Exit intent + Masters Report Beehiiv wiring~~ | SHIPPED | `/api/newsletter` handles `go-exit-intent` + `masters-report` with Beehiiv tag + Resend welcome + CRM upsert. |
| ~~5~~ | ~~Method onboarding quiz~~ | SHIPPED | Live at `/method/onboarding`. 48-cell plan matrix, recommender, results view all built. |

Items **1, 4, 6, 7** remain open, **day 14**. Carried-over
extras: Google Ads 2FA + ad groups 2–4 (split out of #7),
and the `/go` hero video swap (adjacent to #1).

---

## Active blockers (still need Anthony)

### B1. Method course module video scripts — day 14, scope still ambiguous

- 12 of 12 module entries still have `videoYouTubeId: null`.
- `docs/method-module-teleprompter-scripts.md` still
  untracked (seven days now), still 60–90s per module —
  not the 10–15 min the original brief called for.
- Three interpretations still open:
  1. Scope shifted to short intro/companion videos (file is
     the deliverable, just confirm).
  2. Hook-only; the 10–15 min module bodies still need
     writing.
  3. Short video as entry point + long-form MDX underneath.
- **Ask (unchanged):** one Slack reply naming (1), (2),
  or (3). If (1) or (3), pick a batch-record date.

### B2. Hero video for /go — code shelf-ready, recording on you

- `HeroVideo` component still on the shelf, exported from
  `src/components/ui/index.ts`. Renders the designed
  placeholder while `videoYouTubeId` is `null`.
- **Re-verified:** still not imported into
  `src/app/go/page.tsx`. Two-line change for Ted the
  moment an unlisted YouTube ID exists.
- Script `docs/hero-video-script-60s.md` unchanged.
- **Action only you can take:** record the 60-second take.
  Naturally pairs with B1's batch day — same dark-background,
  tight-frame setup.

### B3. Google Ads — three sub-items, all still gated on 2FA (day 14)

- **B3a. Google Ads 2FA on `anthony@`** — still blocked.
  Five-minute action. **Fourteen days running.** Highest-
  leverage item on the list, no closer to resolution than
  on day 1.
- **B3b. Conversion test** — code unchanged at HEAD.
  `GoogleAdsTag.tsx` still on commit `3066511e`;
  diagnostic + `/go` labels wired but unverified for
  fourteen days. Until B3a clears, the early "Ads
  Landing Page View" signal stays in *Needs attention*
  and Google's bidding has nothing more frequent than
  the final 1.4% action to optimise on.
- **B3c. Ad groups 2–4** — still blocked behind B3a.
- **Adjacent code fixes flagged in the funnel audit
  (Ted tasks once you prioritise):**
  - `?source=ads` from the ad landing never reaches Beehiiv
    / CRM because the diagnostic reads `?utm_source`.
    Internal attribution drops silently.
  - Exit-intent popup currently fires on top of the live
    quiz on `/plateau`.

### B4. TrainingPeaks legacy plan build — day 14, status unchanged

- Analysis done: 222 workouts, 21 session types,
  `docs/trainingpeaks-hour-band-variants.md`, 48-cell matrix
  in `docs/method-training-plan-matrix.md`.
- Build of the 48 variants inside TrainingPeaks still
  needs either access granted or a 30-min walkthrough.
- **Decision (same as the last ten mornings):** grant
  TrainingPeaks access to whoever is building the variants,
  or put a 30-min walkthrough on the calendar.

### B5. CRM / external API keys — day 14, status unchanged

- TrainingPeaks API access confirmed in
  `docs/trainingpeaks-plan-analysis.md`.
- `.env.example` slots unchanged: `BEEHIIV_API_KEY`,
  `BEEHIIV_PUBLICATION_ID`, `STRIPE_SECRET_KEY`,
  `STRIPE_WEBHOOK_SECRET`, `STRIPE_RACE_REPORT_PRICE_ID`,
  `RESEND_API_KEY`, `SKOOL_EMAIL`, `SKOOL_PASSWORD`,
  `SKOOL_COMMUNITY_SLUG`.
- Same question as the last fourteen mornings: which of
  these are populated in Vercel, which still need
  rotating / generating?

---

## Today's smallest unblocking moves, ranked by leverage

1. **Google Ads 2FA (B3a)** — still the single
   highest-leverage five minutes on the list. **Fourteen
   days open.** Clears B3b, B3c, and converts the
   funnel-audit findings from theory into measurable
   action.
2. **Confirm the Method video scope (B1)** — one Slack
   reply or one line in this thread. Determines whether B1
   becomes "schedule a record day" or "keep writing".
3. **Record the /go hero video (B2)** — component built;
   you're the only blocker between placeholder and live
   video. Pair with the B1 batch day.
4. **TrainingPeaks access or walkthrough (B4)** — pick one,
   30 minutes.
5. **`.env` audit (B5)** — five minutes in Vercel; clears
   the longest-running ambiguity on the list.

---

## Pattern note

Four consecutive days of zero Anthony-relevant movement
(27 → 28 → 29 → 30 May). The previous two reports both
flagged this pattern explicitly. The signal continues to
fire: **none of B1–B5 will resolve asynchronously.** Ted
shipped eleven commits yesterday — none of them in the
Anthony-blocker surface, because none of them can be.
Every one of these five items needs a calendar slot from
you, not a check-in.

Reiterating yesterday's suggested carve-outs (still the
right shape):

- **5 min today, before your next thing:** Google Ads 2FA
  + `.env` audit (B3a + B5). Both clear with phone in hand.
- **One Slack message today:** Method video scope decision
  (B1). Picks the shape of the next two weeks.
- **Half-day next week, blocked on calendar:** TrainingPeaks
  variant build or walkthrough (B4). Won't happen in the
  gaps between other work.
- **Recording day next week, paired with B1:**
  module videos (if B1 lands on option 1 or 3) + the
  /go hero (B2). One studio setup, two deliverables.

---

## Notes for tomorrow's diff

- Watch whether `HeroVideo` gets imported into
  `src/app/go/page.tsx` and whether `videoYouTubeId` ever
  flips off `null`.
- Watch whether `docs/method-module-teleprompter-scripts.md`
  finally gets committed — seven days untracked.
- If B3a clears, expect a flurry of follow-up code changes
  (UTM normalisation, exit-popup suppression on `/plateau`)
  to surface in the next diff.
- Yesterday's heavy back-catalogue transcription run
  (200 episodes in two commits) plus the Ask-Roadman
  magic-link gate suggests Ted is in indexable-content +
  lead-gen mode. Orthogonal stream — it will keep filling
  the diff without resolving any of B1–B5.
