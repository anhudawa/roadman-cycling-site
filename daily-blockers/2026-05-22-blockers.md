# Morning Blockers — Fri 22 May 2026

Diffed against `daily-blockers-2026-05-21.md` (mid-day audit) and
the prior morning list `2026-05-20-blockers.md`. **New activity
overnight: one commit, `b93b1c51` (21 May 08:23 BST) — Meta
Pixel dedupe.** That's the first commit on HEAD since 20 May
21:48. Working tree still carries the same blog `.mdx` edits +
new audit docs from earlier in the week; none of them touch an
open blocker.

> `project_needs_from_anthony_may13.md` is still not in this
> workspace — re-searched today, the only `anthony*` matches
> are inside an unrelated worktree (`exciting-varahamihira-f74cd1`).
> Statuses below are diffed against the mid-day audit in
> `daily-blockers-2026-05-21.md`, which closed three items from
> the original scheduled-task list.

---

## Closed since the scheduled-task list was written

| Original item | Status | Evidence |
|---|---|---|
| #2 Beehiiv plateau nurture sequences | ✅ LIVE | 4 automations running in Beehiiv (Fuelling Deficit, Strength Gap, Polarisation Gap, Under-Recovered). Open rates 43–62%, 51 total enrolled. Per `daily-blockers-2026-05-21.md`. |
| #3 Exit intent + Masters Report Beehiiv wiring | ✅ SHIPPED | `/api/newsletter` handles both `go-exit-intent` and `masters-report` sources with Beehiiv tagging + Resend welcome + CRM upsert. Per 21 May audit. |
| #5 Method onboarding quiz | ✅ SHIPPED | Live at `/method/onboarding`. 48-cell plan matrix (4 goals × 4 hour tiers × 3 levels), recommender + results view all built. |

So the original 7-item list is down to 4 items still needing
you, plus a couple split out of #6/#7 below.

---

## Active blockers (still need Anthony)

### 1. Method course module video scripts — STILL OUTSTANDING (6th day)
- `src/lib/method/modules.ts` re-checked at HEAD: **12 of 13**
  module rows still have `videoYouTubeId: null`. Only one
  module has a video ID populated. No change overnight.
- Scripted long-form assets in `docs/` are still the short-form
  bundle only: `teleprompter-scripts-13-may.md` (8.0 KB),
  `teleprompter-clean.txt` (7.4 KB), `go-video-script.md`,
  `hero-video-script-60s.md` (added 19 May). No new
  module-length scripts have landed in `docs/` since 13 May.
- **Ask (6th time):** for each of the 12 outstanding modules
  (`01-where-you-actually-are` → `12-not-done-yet`) — rough
  script written, needs writing, or already filmed waiting on
  upload? Plus a target batch-record date so the missing
  scripts can be drafted against a deadline.

### 2. Hero video for /go — STILL OUTSTANDING
- Script delivered: `docs/hero-video-script-60s.md` (60-second
  cut, last touched 19 May). Commit `866c9f99` on 20 May added
  it to the repo.
- **Action only you can take:** record the take. Once it's
  filmed and uploaded the script doc can be retired and the
  `/go` hero swapped.

### 3. Google Ads — three sub-items, all gated on 2FA (still blocked)
The mid-day audit on 21 May reframed the original "test the
conversion" ask as three blocked items, all sitting behind one
auth wall:

- **3a. Google Ads 2FA on `anthony@`** — you can't get into
  Google Ads at all until this is set up. This is the
  unblocker for 3b and 3c.
- **3b. Conversion test** — code-side plumbing re-verified
  today and unchanged: `GoogleAdsTag.tsx:3` loads
  `AW-18123737652`; `ResultsAnalytics.tsx:15` fires
  `AW-18123737652/WDZ_CNiOvKwcELSUicJD` on diagnostic results;
  `AdsLandingAnalytics.tsx:15` fires the `/go` landing label
  once per session. **Six days unverified** — the longer this
  sits, the more ad spend you can't attribute.
- **3c. Ad groups 2–4** — can't be built until 3a is resolved.

### 4. TrainingPeaks legacy plan structure — STATUS CONFLICT
- The 21 May audit lists "TrainingPeaks legacy plan structure"
  as still blocked (need access or walkthrough).
- The 20 May morning report had this **closed**: 222 workouts /
  21 session types extracted, hour-band variants documented in
  `docs/trainingpeaks-hour-band-variants.md`, with the *build*
  of the 48 variants noted as the next implementation phase.
- **Reading the two together:** the analysis is done, but
  building the 48 plan variants inside TrainingPeaks still
  needs either access to the legacy plan structures or a
  walkthrough from you. So the *blocker* on you is real, but
  it's about the *build* phase, not the analysis.
- **Decision needed:** grant TrainingPeaks access to whoever
  is building the variants, or schedule a 30-min walkthrough.

### 5. CRM API keys — STATUS UNCHANGED (6th day)
- TrainingPeaks API access already confirmed
  (`docs/trainingpeaks-plan-analysis.md`).
- `.env.example` slots unchanged: `BEEHIIV_API_KEY`,
  `BEEHIIV_PUBLICATION_ID`, `STRIPE_SECRET_KEY`,
  `STRIPE_WEBHOOK_SECRET`, `STRIPE_RACE_REPORT_PRICE_ID`,
  `SKOOL_EMAIL`, `SKOOL_PASSWORD`, `SKOOL_COMMUNITY_SLUG`.
- Can't read prod `.env` from here. Same question as the
  last six mornings: which of these are populated in Vercel,
  which still need rotating / generating?

---

## Resolved / not blocking you

- **Beehiiv plateau sequences** — live, performing (see closed
  table above). The v2 expansion (3 → 7 emails per sequence)
  in `docs/plateau-diagnostic-email-sequences-v2.md` is a
  future optimization, not a blocker.
- **Exit intent + Masters Report wiring** — shipped.
- **Method onboarding quiz** — shipped.
- **Meta Pixel duplicate init** — fixed yesterday in
  `b93b1c51`.
- **Trustpilot free-plan compliance** — fixed Tue in `9fddbef7`.

---

## Diff vs. mid-day audit 21 May

| # | Item | 21 May (mid-day) | 22 May |
|---|---|---|---|
| 1 | Method module video scripts | BLOCKED | BLOCKED (no change) |
| 2 | Hero video for /go | BLOCKED | BLOCKED (no change) |
| 3a | Google Ads 2FA | BLOCKED | BLOCKED (no change) |
| 3b | Google Ads conversion test | BLOCKED on 3a | BLOCKED on 3a |
| 3c | Google Ads ad groups 2–4 | BLOCKED on 3a | BLOCKED on 3a |
| 4 | TrainingPeaks legacy plan build | BLOCKED | BLOCKED (no change) |
| 5 | CRM API keys (Stripe / Skool / Beehiiv / TP) | BLOCKED | BLOCKED (no change) |

Nothing on the blocker list moved overnight. The one commit
that landed (`b93b1c51`) was Meta Pixel dedupe — useful, but
not on this list.

---

## Suggested order if you can grab 30 minutes today

1. **Google Ads 2FA** (5 min) — single highest-leverage item.
   Unlocks 3b and 3c, and stops a week's worth of unverified
   ad spend in its tracks.
2. **Pick the TrainingPeaks unblock** (2 min) — either grant
   access or put a 30-min walkthrough on the calendar. Either
   one moves the 48-variant build off your plate.
3. **Decide on the Method script status** (10 min) — even just
   marking the 12 modules as "scripted / needs writing / filmed
   not uploaded" turns this from an open question into a
   workable backlog.

---

## Tomorrow's nudge

If `project_needs_from_anthony_may13.md` lives somewhere
outside the workspace, share the path and the next diff will
run against the canonical source. If any of items 1, 2, 4, 5
have been progressed outside the repo (Google Ads console,
TrainingPeaks, Vercel env), a one-liner closes them off the
list.
