"use client";

/**
 * Custom smooth cursor disabled — reverted to native OS cursor after user
 * complaints about responsiveness. The SmoothCursor component, CSS rules in
 * globals.css, and this wrapper are preserved so the feature can be
 * re-enabled later if desired.
 */
export function SmoothCursorWrapper() {
  return null;
}
